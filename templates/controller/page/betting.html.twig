<div class="panel-body center-block" style="width: 50%">
    {% for message in app.flashes(form.vars.id) %}
        <div class="alert alert-success">
            {{ message|trans }}
        </div>
    {% endfor %}
    {% form_theme form 'bootstrap_3_layout.html.twig' %}
    {{ form(form) }}
</div>
